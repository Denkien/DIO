//Estrutura de dados. tipo primitivo = number, string, boolean, null. não primitivo = arrays e objetos.

// string = texto. "pipipi popopo". tem o `q da pra fazer string e o ${q pega variavel}`


let nome = "marquin";
let sobrenome = "zé"

let concatenado = nome.concat(sobrenome);

let concatenado2 = `${nome} ${sobrenome}`

console.log(concatenado, concatenado2);


//numbers da pra puxar o Math.coisas pra fazer operações especificas. pra mostrar % na tela, só usando string, por causa do sinal lá q serve pra outra coisa.



console.log(Math.PI);


//boolean é true ou false.

let verdade= 3===5

console.log(!verdade);